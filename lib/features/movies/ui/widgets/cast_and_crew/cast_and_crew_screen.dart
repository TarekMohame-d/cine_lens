import 'package:flutter/material.dart';

class CastAndCrewScreen extends StatelessWidget {
  const CastAndCrewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Center(
          child: Text('avadvadvadva'),
        ),
      ),
    );
  }
}
